numpy>=1.23
Pillow>=10.0
PyYAML>=6.0
matplotlib>=3.7
ipywidgets>=8.0
scipy>=1.10
pytest>=7.4
torch>=2.0

# Desktop GUI runtime (inference GUI + deterministic pair demo GUI)
PySide6>=6.6

# Optional EBSD ecosystem packages for advanced deterministic baseline extensions
# (kept optional to avoid forcing heavyweight installs in basic workflows):
# kikuchipy>=0.11
# hyperspy>=2.0
# pyebsdindex>=0.3
# diffpy.structure>=3.1
